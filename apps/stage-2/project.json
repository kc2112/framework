{
  "name": "stage-2",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/stage-2/src",
  "projectType": "application",
  "tags": [
    "type:app",
    "scope:api"
  ],
  "implicitDependencies": [
    "express-app"
  ],
  "targets": {
    "serve": {
      "command": "node apps/stage-2/src/server.js",
      "options": {
        "cwd": "{workspaceRoot}",
        "env": {
          "PORT": "3002"
        }
      }
    },
    "test": {
      "command": "node --experimental-vm-modules node_modules/jest/bin/jest.js --testPathPattern=apps/stage-2 --coverage=false",
      "options": {
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "command": "eslint apps/stage-2",
      "options": {
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
