{
  "name": "stage-1",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/stage-1/src",
  "projectType": "application",
  "tags": [
    "type:app",
    "scope:api"
  ],
  "implicitDependencies": [
    "express-app"
  ],
  "targets": {
    "serve": {
      "command": "node apps/stage-1/src/server.js",
      "options": {
        "cwd": "{workspaceRoot}",
        "env": {
          "PORT": "3001"
        }
      }
    },
    "test": {
      "command": "node --experimental-vm-modules node_modules/jest/bin/jest.js --testPathPattern=apps/stage-1 --coverage=false",
      "options": {
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "command": "eslint apps/stage-1",
      "options": {
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
